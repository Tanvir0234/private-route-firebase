import React from 'react';
import useAuth from '../../Hooks/useAuth';

const Payment = () => {
    const user=useAuth()
    return (
        <div>
            <h2>Payment</h2>
            
        </div>
    );
};

export default Payment;